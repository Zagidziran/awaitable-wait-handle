<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net5.0</TargetFramework>
    <RootNamespace>Zagidiziran.Concurrent.AwaitableWaitHandle</RootNamespace>
    <AssemblyName>Zagidiziran.Concurrent.AwaitableWaitHandle</AssemblyName>
  </PropertyGroup>

  <PropertyGroup>
    <NuspecFile>Zagidziran.Concurrent.AwaitableWaitHandle.nuspec</NuspecFile>
    <NuspecProperties>version=$(PackageVersion);configuration=$(Configuration)</NuspecProperties>
  </PropertyGroup>

</Project>
